#include <stdio.h>
#include <stdlib.h>
#include<math.h>

int main()
{

    int n,a,b,c;
    printf("enter the value of n");
    scanf("%d",&n);
    a=pow(n,1);
         b=pow(n,2);

             c=pow(n,3);

    printf("the value is %d\n%d\n%d\n",a,b,c);
    return 0;
}
